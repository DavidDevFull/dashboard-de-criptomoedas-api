export const containerCompareCoin = (container: HTMLDivElement) => {
   container.insertAdjacentHTML("beforeend", `
      <section class="containerCompareCoin">
         <span>🧩 Component de comparação ainda em desenvolvimento 🛠</span>
      </section>
   `);
}